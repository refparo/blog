[package]
name = "blog"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.44"
typst = "0.14.2"
typst-html = "0.14.2"
typst-kit = "0.14.2"

[profile.release]
lto = "fat"
panic = "abort"
